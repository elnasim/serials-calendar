server {
    listen 80;
    listen [::]:80;
    server_name localhost;
    charset utf-8;
    root /app;
    index index.html;

    location / {
      root /app;
      try_files $uri /index.html;
    }
}

